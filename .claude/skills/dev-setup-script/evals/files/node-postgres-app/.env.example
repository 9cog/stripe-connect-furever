# Postgres — matches the service in docker-compose.yml
DATABASE_URL="postgresql://acme:acme@localhost:5433/acme_orders?schema=public"

# Sendgrid, for order confirmation emails (leave blank to log to console)
SENDGRID_API_KEY=""

PORT=4000
